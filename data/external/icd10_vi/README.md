# data/external/icd10_vi/ — untracked Vietnamese ICD-10 table

Place a locally acquired Vietnamese ICD-10 table here (CSV/TSV/XLSX-exported-CSV).
Git-ignored. Never committed.

The exact organizer source/version/format is undisclosed (see
`configs/organizer/unresolved_policies_v1.yaml::UNRES-ICD-SOURCE`). The loader is
configurable (column mapping) rather than hard-coded to one source.
