version: gliner-adapter-v1
expert_id: E6_gliner_open_type
status: IMPLEMENTED_UNTRAINED
enabled: false
local_model_path: ""
model_revision: UNPINNED_UNTRAINED_REQUIRES_OPERATOR_PIN
checkpoint_sha256: UNAVAILABLE_UNTRAINED
threshold: 0.5
label_descriptions:
  version: gliner-label-descriptions-v1
  MEDICATION: "Medication, drug, dose, route, frequency, or treatment mention."
  DIAGNOSIS: "Disease, disorder, confirmed diagnosis, or clinical condition."
  SYMPTOM: "Symptom, sign, patient complaint, or manifestation."
  TEST_NAME: "Laboratory, imaging, or clinical test name."
  TEST_RESULT: "Test result, numeric value, measurement, or interpretation."
runtime:
  no_auto_download: true
  require_local_path: true
  reject_malformed_spans: true
  exact_offset_validation: true
